// src/app/components/ProductFilters.tsx
export { default } from "./FiltersBar";
// Alias the type so product + service imports don't collide
export type { Filters as ProductFilters } from "./FiltersBar";
