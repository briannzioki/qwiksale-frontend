// tests/e2e/dashboard-auth.spec.ts
import { test } from "@playwright/test";

test.describe("Dashboard routing from account menu", () => {
  test("Admins: Dashboard menu routes to /admin", async () => {
    // names only
  });

  test("Users: Dashboard menu routes to /dashboard", async () => {
    // names only
  });
});
