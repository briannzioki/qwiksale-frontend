// tests/e2e/session-ui-vs-api.spec.ts
import { test } from "@playwright/test";

test.describe("Session UI vs API chip rendering", () => {
  test("Admin: exactly one chip visible inside the account button (role chip)", async () => {
    // names only
  });

  test("User: exactly one chip visible inside the account button (plan chip)", async () => {
    // names only
  });
});
